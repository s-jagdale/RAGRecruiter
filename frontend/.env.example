# Required — app will not start without these (see app/config.py)
WATSONX_API_KEY=
WATSONX_REGION_URL=
WATSONX_MODEL_ID=
WATSONX_EMBEDDING_MODEL_ID=

# Optional — voice-answer feature only degrades gracefully if these are absent
STT_API_KEY=
STT_URL

# Recommended for production — falls back to an insecure default otherwise
JWT_SECRET_KEY=
